# RAG-QA-System
A document-grounded QA system built with RAG pipeline over a 400-page ML textbook, using OpenAI embeddings, Pinecone vector search, and a Streamlit interface for interactive querying.
